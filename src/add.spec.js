/*import add from './add';
import sub from './sub';*/
import head from './head';
/*describe('Add', () => {

    it('Sum of 0 and 0 is 0', () => {
        expect(add(0, 0)).toEqual(0);
    });

    it('Sum of 1 and 0 is 1', () => {
        expect(add(1, 0)).toEqual(0);
    });

})
describe('Sub', () => {

    it('Sum of 0 and 0 is 0', () => {
        expect(sub(0, 0)).toEqual(0);
    });

    it('Sum of 1 and 0 is 1', () => {
        expect(sub(1, 0)).toEqual(0);
    });

})*/
describe('head', () => {

    it('head of [1,2,3,4] is 1', () => {
        expect(head([1,2,3,4])).toEqual(1);
    });

})
